# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Auth + config
python-dotenv>=1.0.1
pyjwt>=2.10.1
bcrypt>=4.1.3
email-validator>=2.2.0

# Models / validation
pydantic>=2.6.4

# MongoDB (async)
motor>=3.6.0
pymongo>=4.9.0

# HTTP client (link preview)
requests>=2.31.0

# S3-compatible client for the Cloudflare R2 storage backend
# (only imported when STORAGE_BACKEND=r2; safe to leave installed for local dev)
boto3>=1.34.0

# Document text extraction
pypdf>=4.0.0
python-docx>=1.1.0
python-pptx>=1.0.0

# LLM (replaces emergentintegrations)
anthropic>=0.40.0

# Tests
pytest>=8.0.0
reportlab>=4.0.0  # used by test_iteration2.py to build PDFs
